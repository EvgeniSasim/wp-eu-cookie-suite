# CC-12 Banner customizer

```markdown
Task CC-12 — Banner appearance customizer.

Depends on CC-11 merged.

## Requirements
1. Admin Banner tab sections: Layout (box/bar), Position (bottom-left, bottom-center, bottom-right, center), Theme (light/dark), primary color (hex).
2. Persist in `wpeu_cs_settings['banner_ui']`.
3. Map to CookieConsent `guiOptions` and CSS variables / `cssVariables` if supported.
4. Live preview iframe or JS preview on admin page (simple is fine).
5. Optional: custom CSS textarea (sanitized).

One PR. No external page builders.
```
